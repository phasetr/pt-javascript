{
	"name": "hono-api",
	"scripts": {
		"dev": "wrangler dev --port 3000  --persist-to ../../.wrangler-persist",
		"deploy": "wrangler deploy --minify",
		"cf-typegen": "wrangler types --env-interface CloudflareBindings"
	},
	"dependencies": {
		"db": "workspace:*",
		"drizzle-orm": "^0.43.0",
		"hono": "^4.7.7"
	},
	"devDependencies": {
		"wrangler": "^4.13.2"
	}
}
