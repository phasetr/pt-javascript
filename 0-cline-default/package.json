{
  "name": "<proj-name>-monorepo",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "pnpm -r build",
    "test": "pnpm -r test",
    "dev": "pnpm --filter=@cbul/hono-api dev"
  },
  "packageManager": "pnpm@10.7.0"
}
